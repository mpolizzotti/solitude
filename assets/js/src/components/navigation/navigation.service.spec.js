import {NavigationService} from './navigation.service';

describe('NavigationService Class', () => {
    'use strict';

    let navigationService;

    beforeEach(() => {
        navigationService = new NavigationService();
    });

    afterEach(() => {
        navigationService = undefined;
    });
});
