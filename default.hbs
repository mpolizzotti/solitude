<!DOCTYPE html>
<html>
    <head>
        {{!-- Document Settings --}}
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />

        {{!-- Page Meta --}}
        <title>{{meta_title}}</title>
        <meta name="description" content="{{meta_description}}" />

        {{!-- Mobile Meta --}}
        <meta name="HandheldFriendly" content="True" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />

        {{!-- Brand icon --}}
        <link rel="shortcut icon" href="{{asset "favicon.ico"}}">

        {{!-- Styles'n'Scripts --}}
        <link rel="stylesheet" type="text/css" href="{{asset "css/solitude.css"}}" />

        {{!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries --}}
        {{!-- WARNING: Respond.js doesn't work if you view the page via file:// --}}
        {{!--[if lt IE 9]>
          <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
          <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
        <![endif]--}}

        {{!-- Ghost outputs important style and meta data with this tag --}}
        {{ghost_head}}
    </head>
    <body class="solitude {{body_class}}">

        {{!-- Navigation
        {{navigation}} --}}

        {{!-- Site wrapper --}}
        <div class="site-wrapper">
            {{{body}}}

            {{!-- Subfooter --}}
            {{> "sub-footer"}}

            {{!-- Footer --}}
            {{> "footer"}}
        </div>

        {{!-- jQuery needs to come before `{{ghost_foot}}` so that jQuery can be used in code injection. --}}
        <script type="text/javascript" src="{{asset "js/lib/jquery.min.js"}}"></script>
        <script type="text/javascript" src="{{asset "js/lib/lodash.min.js"}}"></script>
        <script type="text/javascript" src="{{asset "js/lib/bootstrap.min.js"}}"></script>

        {{!-- Ghost outputs important scripts and data with this tag. --}}
        {{ghost_foot}}

        {{!-- The main JavaScript file for the solitude theme. --}}
        <script type="text/javascript" src="{{asset "js/min/solitude.min.js"}}"></script>
    </body>
</html>
